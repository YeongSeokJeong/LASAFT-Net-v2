# @package _global_

# to execute this experiment run:
# python run.py experiment=example_simple.yaml

defaults:
  - override /trainer: single_gpu
  - override /dataset: multi_condition
  - override /callbacks: default
  - override /model: v1
  - override /logger: wandb
